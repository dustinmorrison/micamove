<!-- <h1>Listing products</h1>

<table>
  <tr>
    <th>Category</th>
    <th>Description</th>
    <th>Name</th>
    <th>Color</th>
    <th>Price</th>
    <th>Shoplink</th>
    <th>Image1</th>
    <th>Image2</th>
    <th>Image3</th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.category %></td>
    <td><%= product.description %></td>
    <td><%= product.name %></td>
    <td><%= product.color %></td>
    <td><%= product.price %></td>
    <td><%= product.shoplink %></td>
    <td><%= product.image1 %></td>
    <td><%= product.image2 %></td>
    <td><%= product.image3 %></td>
    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', new_product_path %> -->

<div id="products">
  <div class="container-fluid">

    <%= render(:partial => "product-nav") %>

    <div class="row-fluid carousel-products"> 
      <div class="span12">
        <div id="myCarousel" class="carousel slide">
          <!-- Carousel items -->
          <div class="carousel-inner">
            <div class="active item">
              <img src="assets/product_carousel_deck.jpg"></img>
            </div>
            <div class="item">
              <img src="assets/product_carousel_plank.jpg"></img>
            </div>
            <div class="item">
              <img src="assets/product_carousel_drift.jpg"></img>
            </div>
          </div>
          <!-- Carousel nav -->
          <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
          <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
        </div>
      </div>
    </div>

    <div class="row-fluid">
      <div class="span12">
        <ul class="product-list">
          <% @products.each do |product| %>
          <li class="product-listing">
            <div><%= link_to image_tag(product.image1), product %></div>
            <div><%= link_to product.name, product %> / <%= link_to product.color, product %></div>
            <div>$<%= link_to (product.price), product %></div>
          </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
