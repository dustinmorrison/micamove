<%= form_for(@store_location) do |f| %>
  <% if @store_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_location.errors.count, "error") %> prohibited this store_location from being saved:</h2>

      <ul>
      <% @store_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
   <!--
  <div class="field">
    <%= f.label :latitude %><br />
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br />
    <%= f.text_field :longitude %>
  </div>
   -->
  <div class="actions">
    <%= f.submit :class=>'btn btn-success' %>
  </div>
<% end %>
